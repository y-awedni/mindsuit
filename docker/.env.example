# Copy to docker/.env and adjust if needed. This file is committed; docker/.env is NOT.
# These values must match app/config/parameters.yml (generated from parameters.yml.dist).

# --- MariaDB container ---
MARIADB_DATABASE=mindsuit
MARIADB_USER=mindsuit
MARIADB_PASSWORD=mindsuit
MARIADB_ROOT_PASSWORD=root
