{% extends 'base.html.twig' %}

{% block title %}{{ parent()~' - Les avoirs de client ' }}{% endblock %}
{% block body %}
    {% set currentPath = path(app.request.attributes.get('_route'),app.request.attributes.get('_route_params')) %}
    {% set currentPath=currentPath~'?a=b' %}
    {% if app.request.get('limit') is not null %}
        {% set currentPath=currentPath~'&limit='~app.request.get('limit') %}
    {% endif %}
    {% if app.request.get('sort') is not null %}
        {% set currentPath=currentPath~'&sort='~app.request.get('sort') %}
    {% endif %}
    {% if app.request.get('direction') is not null %}
        {% set currentPath=currentPath~'&direction='~app.request.get('direction') %}
    {% endif %}
    {% set currentPath=currentPath~'&codeAvoir='~app.request.get('codeAvoir') %}
    {% set currentPath=currentPath~'&startDateCreation='~app.request.get('startDateCreation') %}
    {% set currentPath=currentPath~'&endDateCreation='~app.request.get('endDateCreation') %}
    {% set currentPath=currentPath~'&codeArticle='~app.request.get('codeArticle') %}
    {% set currentPath=currentPath~'&designationArticle='~app.request.get('designationArticle') %}
    <div class="container-fluid container-fluid-fixed-top">
        {% include 'fiche/client/menu.html.twig' with { 'client':client } %}
        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <div class="count">
                            {{ total }} Avoirs du client <a href="{{ path('client_show',{'id':client.id} ) }}">{{ client }}</a>
                        </div> 
                    </div>
                    <!-- /.panel-heading -->
                    <div class="panel-body">
                        <div class="panel panel-default">
                            <div class="panel-heading">
                                <h4 class="panel-title">
                                    <a data-toggle="collapse" data-parent="#accordion" href="#collapseOne"><span class="glyphicon glyphicon-chevron-down"></span> Filtrage</a>
                                </h4>
                            </div>
                            <div id="collapseOne" class="panel-collapse collapse">
                                <div class="panel-body">
                                    <form method="GET">
                                        <div class="row">
                                            <div class="col-sm-6 ">
                                                <div class="row">
                                                    <div class="col-md-6">
                                                        <div class="form-group">
                                                            <label for="codeAvoir">Référence avoir</label>
                                                            <input class="form-control" name="codeAvoir" id="code" value="{{ app.request.get('codeAvoir')}}">
                                                        </div>
                                                    </div>
                                                    <div class="col-md-6">
                                                        <div class="form-group">
                                                            <label for="dateCreation">Date</label>
                                                            <div class="input-daterange input-group" id="datepicker">
                                                                <input type="text" class="form-control" value="{{ app.request.get('startDateCreation')}}" name="startDateCreation" />
                                                                <span class="input-group-addon">vers</span>
                                                                <input type="text" class="form-control" value="{{ app.request.get('endDateCreation')}}" name="endDateCreation" />
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="row">
                                                    <div class="col-md-6">
                                                        <div class="form-group">
                                                            <label for="codeArticle">Référence article</label>
                                                            <input class="form-control" name="codeArticle" id="codeArticle" value="{{ app.request.get('codeArticle')}}">
                                                        </div>
                                                    </div>
                                                    <div class="col-md-6">
                                                        <div class="form-group">
                                                            <label for="designationArticle">Désignation article</label>
                                                            <input class="form-control" name="designationArticle" id="designationArticle" value="{{ app.request.get('designationArticle')}}">
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="col-sm-6">
                                                <div class="form-group">
                                                    <label for="limit">Nombre d'éléments par page </label>
                                                    <select name='limit' class="form-control selectpicker" data-live-search="true">
                                                        <option {% if app.request.get('limit')=='10' %}selected{% endif %} value="10">10 éléments par page</option>
                                                        <option {% if app.request.get('limit')=='25' %}selected{% endif %} value="25">25 éléments par page</option>
                                                        <option {% if app.request.get('limit')=='50' %}selected{% endif %} value="50">50 éléments par page</option>
                                                        <option {% if app.request.get('limit')=='100' %}selected{% endif %} value="100">100 éléments par page</option>
                                                    </select>
                                                </div>
                                            </div>
                                        </div>
                                        <button type="submit" class="btn btn-info"><span class="glyphicon glyphicon-filter"></span> Filtrer</button>
                                        <a href="{{path('fiche_client_avoirs',{'id':client.id} )}}" class="btn btn-default"><span class="glyphicon glyphicon-filter"></span> Reinitialiser</a>
                                    </form>
                                </div>
                            </div>
                        </div>
                        <table width="100%" class="table table-striped table-bordered table-hover">
                            <thead>
                                <tr>
                                    {% set direction='ASC' %}
                                    {% if app.request.get('direction') is not null %}
                                        {% set direction=app.request.get('direction')=='DESC'?'ASC':'DESC' %}
                                    {% endif %}
                                    <th><a href="{{ currentPath~"&sort=fav.code&direction="~direction }}">Réference avoir</a></th>
                                    <th><a href="{{ currentPath~"&sort=art.code&direction="~direction }}">Réference article</a></th>
                                    <th><a href="{{ currentPath~"&sort=a.designation&direction="~direction }}">Désignation article</a></th>
                                    <th><a href="{{ currentPath~"&sort=a.qte&direction="~direction }}">Quantité vendus</a></th>
                                    <th><a href="{{ currentPath~"&sort=a.ttc&direction="~direction }}">Montant TTC</a></th>
                                    <th><a href="{{ currentPath~"&sort=a.stock&direction="~direction }}">Retourné</a></th>
                                    <th><a href="{{ currentPath~"&sort=a.reglement&direction="~direction }}">Remboursement</a></th>
                                    <th><a href="{{ currentPath~"&sort=fav.dateCreation&direction="~direction }}">Date</a></th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for element in pagination %}
                                    <tr>
                                        <td>{{ element.favCode }}</td>
                                        <td>{{ element.artCode }}</td>
                                        <td>{{ element.designation }}</td>
                                        <td>{{ element.qte }}</td>
                                        <td>{{ element.ttc }}</td>
                                        <td>{{ element.stock?'oui':'non' }}</td>
                                        <td>{{ element.reglement?'oui':'non' }}</td>
                                        <td>{{ element.dateCreation|date('d-m-Y') }}</td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>

                        {% if countCurrentPage >0 %}
                            <ul class="pagination">
                                <li class="prev {% if currentPage==1 %}disabled{% endif %}" {% if currentPage>1 %}style="cursor:pointer"{% endif %}>
                                    <a {% if currentPage>1 %} href="{{ currentPath }}&offset={{currentPage-2}}" {% endif %} title="{% if currentPage!=1 %}page {{currentPage-1}}{% endif %}" >
                                        « Précédent
                                    </a>
                                </li>
                                {% for i in 1..currentPage %}
                                    {% if i!=currentPage %}
                                        <li>
                                            <a href="{{ currentPath }}&offset={{i-1}}">
                                                {{ i }}
                                            </a>
                                        </li>
                                    {% endif %}
                                {% endfor %}
                                <li class="active">
                                    <a>{{ currentPage }}</a>
                                </li>
                                {% for i in currentPage..lastPage %}
                                    {% if i!=currentPage%}
                                        <li>
                                            <a href="{{ currentPath }}&offset={{i-1}}">
                                                {{ i }}
                                            </a>
                                        </li>
                                    {% endif %}
                                {% endfor %}
                                <li class="next {% if currentPage==lastPage %}disabled{% endif %}" {% if currentPage<lastPage %}style="cursor:pointer"{% endif %}>
                                    <a {% if currentPage<lastPage %} href="{{ currentPath }}&offset={{currentPage}}" {% endif %} title="{% if currentPage!=lastPage %}page {{currentPage+1}}{% endif %}">
                                        Suivant »
                                    </a>
                                </li>
                            </ul>
                        {% endif %}    
                    </div>
                    <div class="panel-footer">
                        <a href="{{ path('fiche_client_avoirs_xls',{
                                'codeAvoir':app.request.get('codeAvoir'),
                                'codeArticle':app.request.get('codeArticle'),
                                'designationArticle':app.request.get('designationArticle'),
                                'client':client.id,
                                'startDateCreation':app.request.get('startDateCreation'),
                                'endDateCreation':app.request.get('endDateCreation') } )}}" class="btn btn-default">
                            <span class="glyphicon glyphicon-download-alt"></span> Exporter en Excel
                        </a>
                    </div>
                </div>
                <!-- totaux -->
                {{ render(controller(
                    'AppBundle:TotauxFicheClient:totauxAvoirs',
                    {
                        'codeAvoir':app.request.get('codeAvoir'),
                        'startDateCreation':app.request.get('startDateCreation'),
                        'endDateCreation':app.request.get('endDateCreation'),
                        'client':client.id,
                        'codeArticle':app.request.get('codeArticle'),
                        'designationArticle':app.request.get('designationArticle')
                    }
                )) }}  
            </div>


        </div>
    </div>
{% endblock %}
{% block javascripts %}
    <script type="text/javascript">
        var myVar = setInterval(myTimer, 2000);
        function myTimer() {
            var x = document.readyState;
            if (x === 'complete') {
                document.getElementById('loader').style.display = 'none';
                $('.container-fluid').removeAttr("style");
            }
        }
    </script>
{% endblock %}