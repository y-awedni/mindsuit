{% extends 'base.html.twig' %}
{% block title %}{{ parent()~' - Avoirs clients' }}{% endblock %}
{% block body %}
    <div class="container-fluid container-fluid-fixed-top">
        <section class="content-header">
            <nav class="navbar navbar-default">
                <div class="container-fluid">
                    <ul class="nav navbar-nav navbar-left">
                        <li>
                            <a data-toggle="modal" data-target="#myModal"><span class="glyphicon glyphicon-plus"></span> Ajouter</a>
                            <!-- Modal -->
                            <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
                                <div class="modal-dialog">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                                            <h4 class="modal-title" id="myModalLabel">Ajouter facture avoir</h4>
                                        </div>
                                        <div class="modal-body">
                                            <form role="form" action="{{path('factureavoir_facture_new_id')}}" method="GET">
                                                <div class="form-group row">
                                                    <div class="col-sm-6" id="factures">
                                                        <label for="factures">Sélectionner une facture</label>
                                                        <select name='facture' class="form-control selectpicker" data-live-search="true" title="tous">
                                                            {% for facture in factures %}
                                                                <option value="{{ facture.id }}">{{ facture.code }}</option>
                                                            {% endfor %}
                                                        </select>
                                                    </div>
                                                </div>

                                                <button type="submit" class="btn btn-info"><span class="fa fa-plus-square"></span> Ajouter facture avoir</button>    
                                            </form>
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-default" data-dismiss="modal">Fermer</button>
                                        </div>
                                    </div>
                                    <!-- /.modal-content -->
                                </div>
                                <!-- /.modal-dialog -->
                            </div>
                        </li>
                    </ul>
                </div>
            </nav> 
        </section>
        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <div class="count">
                            {{ pagination.getTotalItemCount }} factures avoirs
                        </div> 
                    </div>
                    <!-- /.panel-heading -->
                    <div class="panel-body">
                        <div class="panel panel-default">
                            <div class="panel-heading">
                                <h4 class="panel-title">
                                    <a data-toggle="collapse" data-parent="#accordion" href="#collapseOne"><span class="glyphicon glyphicon-chevron-down"></span> Filtrage</a>
                                </h4>
                            </div>
                            <div id="collapseOne" class="panel-collapse collapse">
                                <div class="panel-body">
                                    <form role="form" action="{{path('factureavoir_index')}}" method="GET">
                                        <div class="row">
                                            <div class="col-sm-6 ">
                                                <div class="row">
                                                    <div class="col-md-6">
                                                        <div class="form-group">
                                                            <label for="code">Code</label>
                                                            <input class="form-control" name="code" id="code" value="{{ app.request.get('code')}}">
                                                        </div>
                                                    </div>
                                                    <div class="col-md-6">
                                                        <div class="form-group">
                                                            <label for="facture">Facture</label>
                                                            <select name='facture' class="form-control selectpicker" data-live-search="true">
                                                                <option value=""></option>
                                                                {% for facture in factures %}
                                                                    <option {% if app.request.get('facture')==facture.id %}selected{% endif %} value="{{ facture.id }}">
                                                                        {{ facture }}
                                                                    </option>
                                                                {% endfor %}
                                                            </select>
                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="row">
                                                    <div class="col-md-6">
                                                        <div class="form-group">
                                                            <label for="dateCreation">Date de création</label>
                                                            <div class="input-daterange input-group" id="datepicker">
                                                                <input type="text" class="form-control" value="{{ app.request.get('startDateCreation')}}" name="startDateCreation" />
                                                                <span class="input-group-addon">vers</span>
                                                                <input type="text" class="form-control" value="{{ app.request.get('endDateCreation')}}" name="endDateCreation" />
                                                            </div>
                                                        </div>
                                                    </div>

                                                    <div class="col-md-6">
                                                        <div class="form-group">
                                                            <label for="client">Client</label>
                                                            <select name='client' class="form-control selectpicker" data-live-search="true">
                                                                <option value=""></option>
                                                                {% for client in clients %}
                                                                    <option {% if app.request.get('client')==client.id %}selected{% endif %} value="{{ client.id }}">
                                                                        {{ client }}
                                                                    </option>
                                                                {% endfor %}
                                                            </select>
                                                        </div>
                                                    </div>        

                                                </div>
                                            </div>
                                            <div class="col-sm-6">
                                                <div class="form-group">
                                                    <label for="limit">Nombre d'éléments par page </label>
                                                    <select name='limit' class="form-control selectpicker" data-live-search="true">
                                                        <option {% if app.request.get('limit')=='10' %}selected{% endif %} value="10">10 éléments par page</option>
                                                        <option {% if app.request.get('limit')=='25' %}selected{% endif %} value="25">25 éléments par page</option>
                                                        <option {% if app.request.get('limit')=='50' %}selected{% endif %} value="50">50 éléments par page</option>
                                                        <option {% if app.request.get('limit')=='100' %}selected{% endif %} value="100">100 éléments par page</option>
                                                    </select>
                                                </div>
                                            </div>
                                        </div>
                                        <button type="submit" class="btn btn-info"><span class="glyphicon glyphicon-filter"></span> Filtrer</button>
                                        <a href="{{path('factureavoir_index')}}" class="btn btn-default"><span class="glyphicon glyphicon-filter"></span> Reinitialiser</a>
                                    </form>
                                </div>
                            </div>
                        </div>
                        <table width="100%" class="table table-striped table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th>{{ knp_pagination_sortable(pagination, 'Code', 'a.code') }}</th>
                                    <th {% if pagination.isSorted('f.code') %} class="sorted"{% endif %}>{{ knp_pagination_sortable(pagination, 'Facture', 'f.code') }}</th>
                                    <th>{{ knp_pagination_sortable(pagination, 'Client', 'clt.code') }}</th>
                                    <th {% if pagination.isSorted('a.termine') %} class="sorted"{% endif %}>{{ knp_pagination_sortable(pagination, 'Etat', 'a.termine') }}</th>
                                    <th {% if pagination.isSorted('a.total') %} class="sorted"{% endif %}>{{ knp_pagination_sortable(pagination, 'Total', 'a.total') }}</th>
                                    <th {% if pagination.isSorted('a.dateCreation') %} class="sorted"{% endif %}>{{ knp_pagination_sortable(pagination, 'Date de création', 'a.dateCreation') }}</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for ligne in pagination %}
                                    <tr>
                                        <td>{{ ligne.code }}</td>
                                        <td>
                                            <a href="{{ path('facture_show',{'id':ligne.facture.id } ) }}">{{ ligne.facture }}</a>
                                        </td>
                                        <td>
                                            <a href="{{ path('client_show',{'id':ligne.facture.client.id } ) }}">{{ ligne.facture.client }}</a>
                                        </td>
                                        <td>
                                            <em class="label label-{{ ligne.termine?'success':'warning' }}">
                                                {{ ligne.termine?'Terminé':'En cours' }}
                                            </em>
                                        </td>
                                        <td>{{ ligne.total|number_format('3','.',' ') }}</td>
                                        <td>{{ ligne.dateCreation|date("d/m/Y") }}</td>
                                        <td>
                                            <a class="btn btn-success btn-xs" href="{{ path('factureavoir_show', { 'id': ligne.id }) }}">
                                                <span class="glyphicon glyphicon-eye-open"></span> Afficher
                                            </a>
                                            <a class="btn btn-primary btn-xs" href="{{ path('factureavoir_edit', { 'id': ligne.id }) }}">
                                                <span class="glyphicon glyphicon-edit"></span> Modifier
                                            </a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                        <div class="navigation">
                            {{ knp_pagination_render(pagination) }}
                        </div>
                    </div>
                    <div class="panel-footer">
                        <a href="{{ path('factureavoir_export_xls',{
                                'code':app.request.get('code'),
                                'facture':app.request.get('facture'),
                                'client':app.request.get('client'),
                                'startDateCreation':app.request.get('startDateCreation'),
                                'endDateCreation':app.request.get('endDateCreation') } )}}" class="btn btn-default">
                            <span class="glyphicon glyphicon-download-alt"></span> Exporter en Excel
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    <script type="text/javascript">
        var myVar = setInterval(myTimer, 2000);
        function myTimer() {
            var x = document.readyState;
            if (x === 'complete') {
                document.getElementById('loader').style.display = 'none';
                $('.container-fluid').removeAttr("style");
            }
        }
    </script>
{% endblock %}
