security:
    password_hashers:
        App\Entity\User:
            algorithm: bcrypt
            cost: 13

    role_hierarchy:
        ROLE_ADMIN:       ROLE_USER
        ROLE_SUPER_ADMIN: ROLE_ADMIN

    providers:
        app_users:
            entity:
                class: App\Entity\User
                property: username
                # Users live in the per-tenant database, not the control DB.
                manager_name: tenant

    firewalls:
        dev:
            pattern:  ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            pattern: ^/
            lazy: true
            provider: app_users
            form_login:
                login_path: app_login
                check_path: app_login_check
                enable_csrf: true
                default_target_path: homepage
            logout:
                path: app_logout
                target: app_login
            remember_me:
                secret:   '%env(APP_SECRET)%'
                lifetime: 604800 # 1 week
                path:     /

    access_control:
        - { path: ^/login$, role: PUBLIC_ACCESS }
        - { path: ^/admin/, role: ROLE_ADMIN }
        - { path: ^/, role: ROLE_USER }
